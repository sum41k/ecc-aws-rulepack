terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5"
    }
  }
}

provider "aws" {
  profile = var.profile
  region  = var.default-region

  default_tags {
    tags = {
      CustodianRule    = "ecc-aws-198-elasticsearch_error_logging_to_cloudwatch_enabled"
      ComplianceStatus = "Red"
    }
  }
}