terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5"
    }
  }
}

provider "aws" {
  profile = var.profile
  region  = var.default-region
  default_tags {
    tags = {
      CustodianRule    = "ecc-aws-302-postgresql_log_parser_stats_flag_is_disabled"
      ComplianceStatus = "Green"
    }
  }
}