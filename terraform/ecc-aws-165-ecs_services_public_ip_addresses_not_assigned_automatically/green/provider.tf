terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5"
    }
  }
}

provider "aws" {
  profile = var.profile
  region  = var.default-region

  default_tags {
    tags = {
      CustodianRule    = "ecc-aws-165-ecs_services_public_ip_addresses_not_assigned_automatically"
      ComplianceStatus = "Green"
    }
  }
}