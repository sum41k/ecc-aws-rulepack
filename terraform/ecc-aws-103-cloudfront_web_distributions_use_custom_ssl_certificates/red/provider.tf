terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5"
    }
  }
}

provider "aws" {
  profile = var.profile
  region  = var.default-region

  default_tags {
    tags = {
      CustodianRule    = "ecc-aws-103-cloudfront_web_distributions_use_custom_ssl_certificates"
      ComplianceStatus = "Red"
    }
  }
}