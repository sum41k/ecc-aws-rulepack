terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5"
    }
  }
}

provider "aws" {
  profile = var.profile
  region  = var.default-region

  default_tags {
    tags = {
      CustodianRule    = "ecc-aws-334-kinesis_firehose_delivery_streams_encrypted_using_SSE"
      ComplianceStatus = "Red"
    }
  }
}
