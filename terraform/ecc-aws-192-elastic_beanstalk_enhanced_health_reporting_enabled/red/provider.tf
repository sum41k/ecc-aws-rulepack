terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5"
    }
  }
}

provider "aws" {
  profile = var.profile
  region  = var.default-region

  default_tags {
    tags = {
      CustodianRule    = "ecc-aws-192-elastic_beanstalk_enhanced_health_reporting_enabled"
      ComplianceStatus = "Red"
    }
  }
}