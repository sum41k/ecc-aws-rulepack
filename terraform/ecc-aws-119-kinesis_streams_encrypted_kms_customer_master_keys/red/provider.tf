terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5"
    }
  }
}

provider "aws" {
  profile = var.profile
  region  = var.default-region

  default_tags {
    tags = {
      CustodianRule    = "ecc-aws-119-kinesis_streams_encrypted_kms_customer_master_keys"
      ComplianceStatus = "Red"
    }
  }
}