terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5"
    }
  }
}

provider "aws" {
  profile = var.profile
  region  = var.default-region

  default_tags {
    tags = {
      CustodianRule    = "ecc-aws-104-cloudfront_web_distributions_with_geo_restriction_enabled"
      ComplianceStatus = "Red"
    }
  }
}